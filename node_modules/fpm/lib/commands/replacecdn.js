
"use strict";

var shell = require('shelljs');


function  replacecdn(options) {
    
}

// -------------------

replacecdn.line = function (argv) {
    if(shell.exec("grunt replacecdn").code !== 0){
        console.log("Error: ipm build failed");
    }
};

replacecdn.options = function (argv) {
    
};

replacecdn.completion = function () {
    // TODO:
};

module.exports = replacecdn;