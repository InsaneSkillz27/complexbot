
"use strict";

var shell = require('shelljs');


function  build(options) {
    
}

// -------------------

build.line = function (argv) {
    if(shell.exec("grunt build").code !== 0){
        console.log("Error: ipm build failed");
    }
};

build.options = function (argv) {
    
};

build.completion = function () {
    // TODO:
};

module.exports = build;